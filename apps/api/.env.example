PORT=3001

# Comma-separated list of allowed frontend origins
# For production: set to your Vercel URL, e.g. https://esg-tool.vercel.app
WEB_ORIGIN=http://localhost:3000

# Optional: override the runs output directory (defaults to <repo-root>/runs)
# RUNS_DIR=/app/runs
