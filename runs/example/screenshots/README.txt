This directory would contain example screenshots captured during a real scan.

In a real run, the scanner writes:
  - screenshots/screenshot_<uuid>.png  (full-page screenshot of each form page)
  - videos/<timestamp>.webm             (optional video, if recordVideo=true)

The example report.json references screenshot IDs ev-001 through ev-004.
These placeholder files represent what would be captured from a real scan of example-retailer.com.
